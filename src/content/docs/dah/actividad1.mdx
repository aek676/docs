---
title: Actividad 1
description: En esta actividad se va a desarrollar las diferentes formas de navegar.
---

import { Steps, Code } from '@astrojs/starlight/components';
import RemoteCode from '@/components/RemoteCode.astro';

## 1. Introducción

En esta actividad se va a desarrollar las diferentes formas de navegar entre páginas
en una aplicación Ionic. La navegación es un aspecto fundamental en el
desarrollo de aplicaciones móviles, ya que permite a los usuarios moverse entre
diferentes vistas y funcionalidades de la aplicación.

## 2. Tipos de Navegación

### 2.1. NavController (Recomendado para Ionic)

Es la forma más común para aplicaciones móviles, porque de forma
automática se encarga de gestionar la pila de navegación, lo que facilita la
implementación de transiciones y animaciones entre páginas. Además,
es compatible con las características específicas de Ionic, como el manejo de
gestos y la integración con componentes nativos.

```typescript
import { NavController } from '@ionic/angular/standalone';

constructor(private navCtrl: NavController) {}

goToDetails() {
  this.navCtrl.navigateForward('/details');
}
```

### 2.2. RouterLink (Navegación Declarativa)

Se utiliza directamente en las etiquetas HTML para crear enlaces de navegación.
Es una forma más declarativa de manejar la navegación, ya que se basa en la
estructura del DOM. Es útil para casos simples donde no se requiere
una gestión compleja de la pila de navegación.

```html
<ion-button routerLink="/new-page">Go to New Page</ion-button>

<ion-button [routerLink]="['/new-page', productId]">See Product</ion-button>
```

### 2.3. Router.navigate (Navegación Programática de Angular)

Es la forma nativa de Angular para manejar la navegación. Permite una mayor
flexibilidad y control sobre la navegación. Es útil para casos donde se
requiere lógica adicional antes de navegar a una nueva página,
como puede ser hacer una llamada a una API o cuando trabajamos en servicios donde
no tenemos acceso a NavController.

```typescript
import { Router } from '@angular/router';

constructor(private router: Router) {}

async processAndNavigate() {
  await this.myService.saveData();
  this.router.navigate(['/new-page', this.id], {
    queryParams: { mode: 'edit' },
  });
}
```

### 2.4. Resumen de métodos de navegación

| Método        | Uso Principal          | Ventaja Móvil                                |
|---------------|------------------------|----------------------------------------------|
| NavController | Lógica en el .ts      | Control total de animaciones y Stack         |
| RouterLink    | Directamente en el .html | Más rápido de implementar y leer          |
| Router        | Lógica compleja o Servicios | Estándar de Angular, compatible con Guards |

## 3. Ejercicio

Se debe crear una aplicación Ionic con las siguientes características:

#### 3.1. La aplicación debe tener tres pestañas (Clients, Reservations y Products), siendo la segunda pestaña la que se muestra por defecto al iniciar la aplicación

<Steps>

1. Crear el template HTML

    Crear o modificar el archivo `tabs.page.html` con el componente `ion-tabs`:

    <RemoteCode
      url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/tabs/tabs.page.html"
      lang="html"
      title="src/app/features/tabs/tabs.page.html"
    />

2. Definir las rutas

    Crear el archivo `tabs.routes.ts` con la configuración de rutas:

    <RemoteCode
      url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/tabs/tabs.routes.ts"
      lang="typescript"
      title="src/app/features/tabs/tabs.routes.ts"
    />

3. Configurar las pestañas por defecto

    La pestaña por defecto se configura mediante **redirección del router**:

    <Code
      code={`
   {
      path: '',
      redirectTo: '/tabs/tab2',
      pathMatch: 'full',
      }
      `}
      lang="typescript"
      title="src/app/features/tabs/tabs.routes.ts"
    />

4. Crear el componente TypeScript

    Crear el archivo `tabs.page.ts`:

    <RemoteCode
      url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/tabs/tabs.page.ts"
      lang="typescript"
      title="src/app/features/tabs/tabs.page.ts"
    />

</Steps>

### 3.2. Los iconos de cada pestaña deben ser representativos del tipo de información que se muestra en cada una

<Steps>

1. Agregar iconos en el HTML

   Modificar `src/app/features/tabs/tabs.page.html` para usar `ion-icon`:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/tabs/tabs.page.html"
     lang="html"
     title="src/app/features/tabs/tabs.page.html"
   />

2. Importar iconos en el componente

   En `tabs.page.ts` importar y registrar los iconos:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/tabs/tabs.page.ts"
     lang="typescript"
     title="src/app/features/tabs/tabs.page.ts"
   />

</Steps>

### 3.3. En Clients, cada ítem de la lista debe usar routerLink para enviar su ID, el cual se recibirá y mostrará en la página de detalle mediante @Input()

<Steps>

1. Definir datos del componente

   Crear `src/app/features/clients/clients.page.ts` con la interfaz `Client` y el array de ejemplo:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/clients/clients.page.ts"
     lang="typescript"
     title="src/app/features/clients/clients.page.ts"
   />

2. Crear template del listado con routerLink

    En `src/app/features/clients/clients.page.html` usar `[routerLink]` para navegar al detalle:

    <RemoteCode
      url="<https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/clients/clients.page.html>"
      lang="html"
      title="src/app/features/clients/clients.page.html"
      mark={['[routerLink]']}
    />

3. Crear página de detalle con `@Input`

   En `src/app/features/clients/client-detail.page.ts` usar el decorador `@Input()` para recibir el ID:

   <RemoteCode
     url="<https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/clients/client-detail.page.ts>"
     lang="typescript"
     title="src/app/features/clients/client-detail.page.ts"
     mark={['@Input() id?: string;']}
   />

4. Crear template del detalle

   En `src/app/features/clients/client-detail.page.html` mostrar el ID recibido:

   <RemoteCode
     url="<https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/clients/client-detail.page.html>"
     lang="html"
     title="src/app/features/clients/client-detail.page.html"
     mark={['<h2>Cliente ID: {{ id }}</h2>']}
   />

</Steps>

### 3.4. Pestaña Reservations - Listado y Crear

<Steps>

1. Definir datos del componente

   Crear `src/app/features/reservations/reservations.page.ts` con la interfaz `Reservation` y el array de ejemplo:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/reservations/reservations.page.ts"
     lang="typescript"
     title="src/app/features/reservations/reservations.page.ts"
   />

2. Navegación con navCtrl.navigateForward

   En el componente TypeScript, inyectar `NavController` y crear la función `createReservation()`:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/reservations/reservations.page.ts"
     lang="typescript"
     title="src/app/features/reservations/reservations.page.ts"
     mark={['NavController', 'createReservation()', 'navigateForward']}
   />

3. Template del listado con botón

   En `src/app/features/reservations/reservations.page.html` usar `ion-list` y el botón:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/reservations/reservations.page.html"
     lang="html"
     title="src/app/features/reservations/reservations.page.html"
     mark={['createReservation()']}
   />

</Steps>

### 3.5. Pestaña Products - Listado y Cerrar Sesión

<Steps>

1. Definir datos del componente

   Crear `src/app/features/products/products.page.ts` con la interfaz `Product` y el array de ejemplo:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/products/products.page.ts"
     lang="typescript"
     title="src/app/features/products/products.page.ts"
   />

2. Cerrar Sesión con router.navigate

   En el componente TypeScript, inyectar `Router` y crear la función `logout()` que limpie `localStorage` y navegue a `/login`:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/products/products.page.ts"
     lang="typescript"
     title="src/app/features/products/products.page.ts"
     mark={['Router', 'logout()', 'router.navigate']}
   />

3. Template del listado con botón de logout

   En `src/app/features/products/products.page.html`:

   <RemoteCode
     url="https://raw.githubusercontent.com/aek676/dah-2026/main/src/app/features/products/products.page.html"
     lang="html"
     title="src/app/features/products/products.page.html"
     mark={['logout()']}
   />

</Steps>

### 4.2. Estructura de Rutas

Las rutas se configuran en `src/app/app.routes.ts` y `src/app/features/tabs/tabs.routes.ts`.

#### app.routes.ts

```typescript
export const routes: Routes = [
  {
    path: 'login',
    loadComponent: () => import('./features/login/login.page').then((m) => m.LoginPage),
  },
  {
    path: '',
    loadChildren: () => import('./features/tabs/tabs.routes').then((m) => m.routes),
  },
];
```

#### tabs.routes.ts

```typescript
export const routes: Routes = [
  {
    path: 'tabs',
    loadComponent: () => import('./tabs.page').then((m) => m.TabsPage),
    children: [
      { path: 'tab1', loadComponent: () => import('../clients/clients.page').then((m) => m.ClientsPage) },
      { path: 'tab2', loadComponent: () => import('../reservations/reservations.page').then((m) => m.ReservationsPage) },
      { path: 'tab3', loadComponent: () => import('../products/products.page').then((m) => m.ProductsPage) },
      { path: 'client/:id', loadComponent: () => import('../clients/client-detail.page').then((m) => m.ClientDetailPage) },
      { path: 'reservations/create', loadComponent: () => import('../reservations/reservation-form.page').then((m) => m.ReservationFormPage) },
      { path: '', redirectTo: '/tabs/tab2', pathMatch: 'full' },
    ],
  },
];
```

### 4.4. Página de Login

La página de login está fuera de las pestañas y permite acceder a la aplicación:

```typescript
import { Router } from '@angular/router';

@Component({
  selector: 'app-login',
  // ...
})
export class LoginPage {
  private router = inject(Router);

  login() {
    this.router.navigate(['/tabs']);
  }
}
```
